import 'package:flutter/material.dart';

class TDKPage extends StatelessWidget {
  const TDKPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

        //body:WebView()
        );
  }
}
